# Diagram Viewer 개발 노트

KBS 3238 TV-2 도면(PDF)을 모바일에서 보기 위한 정적 웹 뷰어.
빌드 시스템 없이 순수 HTML/CSS/JS + pdf.js, panzoom(vendor) 조합으로 동작.

## 구조
- `index.html` — 메인 메뉴 (Rack Layout / Video / Audio / Connection Panels / Control)
- `connections.html` — Connection Panels 하위 메뉴 (Video-JF, RF-JF, ANT-JF, AJF, EXT-IO, TIE-IO, CTRL-PP, RF-MIC Layout)
- `viewer.html` — 공통 PDF 뷰어 (쿼리스트링 `?pdf=&title=`로 특정 파일 오픈, `page=`/`hl=`로 특정 페이지·영역 하이라이트 오픈)
- `signal_index.json` — 전 도면 텍스트 라벨 인덱스 (`tools/build_signal_index.py`로 생성, 아래 참고)
- `tools/build_signal_index.py` — PDF에서 라벨+좌표를 추출해 `signal_index.json`을 만드는 빌드 스크립트 (pymupdf 필요, `pip install pymupdf`)
- `vendor/` — pdf.min.js, pdf.worker.min.js, panzoom.min.js
- 실제 도면 PDF 12개가 루트에 정적 파일로 존재

## 완료된 기능
- [x] 모바일 친화적 도면 목록 UI (카드형 메뉴, 다크 테마)
- [x] pdf.js 기반 PDF 렌더링 + panzoom 핀치줌/팬
- [x] 페이지 이동(이전/다음), 리셋(⤢) 버튼
- [x] 렌더링된 페이지 캔버스 캐싱 (재방문 시 즉시 표시)
- [x] 더블탭으로 확대/원복 (탭 지점 기준 줌)
- [x] 핀치줌 후 손 뗄 때 리셋되던 버그 수정
- [x] 리사이즈 시 재렌더링 (디바운스)
- [x] Rack Layout / Connection Panels / Control 섹션 추가
- [x] **도면 간 신호/라벨 상호참조 (search+jump)**: `viewer.html`의 🔍 버튼(또는 캔버스 단일 탭)으로 "BB1", "SPG_1" 같은 라벨을 검색하면, 12개 PDF 전체에서 같은 라벨이 나오는 모든 위치를 목록으로 보여주고, 탭하면 해당 도면/페이지로 이동해 정확한 위치를 빨간 박스로 하이라이트+확대함. `signal_index.json`이 전체 라벨 데이터베이스 역할.
- [x] 검색창에 입력하는 동안 자동완성 제안 표시 (예: "EXT" 입력 시 EXT-IO-A, EXT REF 등 포함된 라벨 목록이 곳수와 함께 드롭다운으로 뜸). `startsWith` 우선 + 짧은 라벨 우선 정렬, 최대 40개.
- [x] 모바일에서 검색으로 다른 도면 페이지로 이동하면 "페이지 1 렌더링 중"에서 멈추는 문제 대응: 캔버스 렌더링 해상도가 `TARGET_PIXEL_BUDGET`(64백만 픽셀) 기준으로 계산되어 A0 사이즈 도면은 캔버스가 9500x6700px까지 커졌었음 — iOS Safari 등 모바일 브라우저의 캔버스/텍스처 한 변 제한(보통 4096px)을 넘어서 렌더링이 멈추거나 실패했을 가능성이 높음. `MAX_CANVAS_DIMENSION = 4096`으로 각 변을 강제로 캡 (`viewer.html`). **주의: 이 트레이드오프로 아주 작은 글자는 최대 줌에서도 예전(64MP)만큼 선명하지 않을 수 있음.** 실기기 재확인 필요 — 여전히 안 되면 사용 중인 기기/브라우저와 정확한 증상(흰 화면인지, 상태 텍스트가 멈춘 채인지 등)을 확인해야 함.
- [x] **`vendor/panzoom.min.js` 자체를 패치함**: 이 라이브러리의 `zoomByRatio()`에 `bounds:true`+`boundsPadding≠1`일 때 확대 배율(scale)이 갱신되지 않고 위치(x/y)만 바뀌는 버그가 있었음 — 마우스 휠/트랙패드로 확대할 때 커서 위치가 아니라 엉뚱한 곳으로 계속 움직이기만 하고 실제로는 잘 확대되지 않는 증상의 원인이었음 (내 하이라이트 자동 확대 기능에서도 같은 버그로 줌이 안 먹혔던 걸 먼저 발견함). `transform.scale*=ratio;keepTransformInsideBounds();`로 정상 순서로 고침 — 휠줌/핀치줌/더블탭줌 전부 이 함수를 공유하므로 한 번에 다 고쳐짐. **주의: panzoom.min.js를 나중에 새 버전으로 통째로 교체하면 이 패치가 사라지니, 교체 시 이 버그 재확인 필요.**
- [x] 하이라이트 박스의 테두리/여백이 글자를 가리는 문제 수정: 하이라이트 박스가 화면 배율(zoom)에 따라 같이 커지는 요소 안에 있다 보니, 고정 CSS px로 지정한 테두리 두께와 여백이 확대할수록(최대 30배까지) 같이 커져서 글자를 통째로 덮어버렸음. 화면상 항상 일정한 크기(여백 5px)로 보이도록 현재 줌 배율로 나눠서 역보정하도록 수정 (`applyHighlight()`). 하이라이트 이동 시 자동 확대 배율도 항상 그 페이지의 최대 선명 배율(`curMaxZoom`)까지 확대하도록 변경.
- [x] 사용자 피드백 반영: 테두리는 아예 없애고 반투명 빨간 배경(펄스 애니메이션)만 남김 (`#highlightBox`에서 `border` 제거).
- [x] `MAX_CANVAS_DIMENSION`을 4096→**8192**로 상향: 4096으로는 작은 CAD 글자가 최대 줌에서도 너무 흐릿했음(화질 저하 피드백). 대신 여러 페이지를 오가며 생기는 메모리 누적을 막기 위해 `MAX_CACHED_PAGES = 3`으로 페이지별 풀해상도 캐시 캔버스를 최대 3장만 유지하고 오래된 것부터 삭제하도록 함 (`pageCache` FIFO 방식 eviction). 모바일에서 8192 해상도가 실제로 안전한지는 아직 실기기로 확인 못함 — 만약 렌더링이 다시 멈추면 이 값을 낮춰야 함.
- [x] 데스크톱에서 라벨 찾기가 **더블클릭**에만 반응하도록 변경 (싱글클릭은 그냥 클릭/드래그로 남김). 이전엔 `canvas`의 `click` 이벤트(마우스 클릭과 모바일 탭 둘 다에서 발생)에 279ms 디바운스로 라벨찾기를 걸어놔서, 데스크톱에서 클릭할 때마다 자꾸 검색 결과가 떴었음. 지금은 데스크톱은 `dblclick` 전용 리스너로, 모바일 싱글탭은 `setupDoubleTap()`의 `touchend` 안에서 별도 타이머로 처리 — 두 입력 방식을 완전히 분리함.
- [x] **한 페이지에 같은 라벨이 여러 곳 있으면 한 번에 모두 하이라이트**: 기존엔 하이라이트 박스가 하나뿐이라 목록에서 하나씩 눌러야 다음 위치를 볼 수 있었음. `#highlightBox` 단일 요소를 `#highlightLayer` 컨테이너 + `.hlBox` 여러 개로 바꾸고, 검색/탭으로 목록(`openSheet`)이 열릴 때 현재 페이지에 있는 occurrence를 전부 모아 `focusOnBboxes()`로 한 번에 표시(여러 개면 전부 화면에 들어오도록 줌을 자동으로 맞춤). 목록의 특정 항목을 클릭하면 그 한 곳만 최대 배율로 확대. 다른 파일로 넘어갈 때도 `norm` 값을 URL에 같이 넘겨서, 도착한 페이지에 같은 라벨이 여러 곳 있으면 거기서도 전부 하이라이트되게 함.
- [x] (검토 후 폐기) BB1 신호 경로를 박스+화살표로 수작업 큐레이션하는 방식(`trace-bb1.html`)도 만들어 검색+점프 방식과 비교했으나, 신호 하나하나 수작업 조사가 필요해 확장성이 없다고 판단 → **검색+점프 방식 하나로 통일하기로 결정**하고 데모 페이지는 제거함.

## 신호 상호참조 관련 중요 메모 (다음에 이어서 작업할 때 필수로 알아야 할 것)
- CAD에서 내보낸 이 PDF들은 페이지 회전(rotation 90/270도)이 걸려 있음. PyMuPDF의 `page.get_text('words')` 좌표는 **회전이 적용되지 않은 원본 좌표계**라서, 화면에 실제 렌더링되는(=pdf.js가 보여주는) 좌표와 다름. 반드시 `fitz.Rect(...) * page.rotation_matrix`로 변환해야 함. (`tools/build_signal_index.py`의 `extract_labels()`에 이미 반영됨 — 이 로직 건드릴 때 절대 빼먹지 말 것. 한 번 이 버그로 하이라이트가 완전히 엉뚱한 곳에 찍혔던 적 있음.)
- 라벨은 CAD 특성상 "BB"+"1"처럼 여러 줄로 쪼개져 있는 경우가 많음. `(block_no, line_no)`로 묶어서 하나의 라벨로 합치는 방식으로 처리함 (예: "BB"+"1" → "BB 1", "BARS"+"HD" → "BARS HD").
- 신호 이름이 같아도(`normalize()`로 공백/언더스코어/하이픈 제거 후 비교) **실제로 전선이 연결됐다는 뜻은 아님** — 같은 텍스트 라벨이 어디에 또 나오는지 찾아주는 것뿐. 도면끼리는 대부분 블록/포트 이름(`EXT-IO-A`, `D23_01` 등)이 반복되는 방식으로 연결되어 있고, 케이블 번호(`CA/25` 같은)는 대부분 같은 시트 내부의 그리드 참조라 다른 파일로는 안 이어짐.
- 새 PDF를 추가하거나 기존 PDF를 교체하면 `python tools/build_signal_index.py`를 다시 실행해서 `signal_index.json`을 갱신해야 함.
- `signal_index.json`은 약 3MB (라벨 4만5천개). 정적 호스팅 시 gzip이 켜져 있는지 확인하면 좋음.

## 알려진 이슈 / 개선 후보
- [ ] `viewer.html`에서 파일명을 브라우저 히스토리에 남기지 않아 새로고침 시 스크롤/줌 위치 초기화됨 — 필요하면 상태 유지 검토
- [ ] PDF 파일명이 매우 길고 규칙이 제각각(A0/A1/A2, D01/D03 등) — 신규 도면 추가 시 실수하기 쉬움. `index.html`/`connections.html`에 하드코딩된 카드 목록을 데이터 배열 기반으로 리팩터링하면 유지보수 편함 (`viewer.html`의 `DIAGRAM_TITLES`와 중복되고 있음, 공용 데이터 파일로 합칠 것)
- [ ] 오프라인/캐시 대응 없음 — 반복 방문 시 매번 재다운로드. Service Worker로 PDF 캐싱하면 현장에서 네트워크 느릴 때 도움
- [ ] 페이지가 여러 장인 PDF에서 페이지 점프(썸네일 목록) 기능 없음, 현재는 이전/다음만 가능
- [ ] 신호 상호참조 검색이 부분일치(포함 검색)일 때 결과가 너무 많이 나올 수 있음 (예: "BB" 검색 시 관련없는 라벨까지 포함) — 필요하면 결과 개수 제한/정렬 개선

## 향후 개발 아이디어 (우선순위 미정)
- [ ] 다크/라이트 테마 토글
- [ ] 즐겨찾기 / 최근 본 도면
- [ ] 주석(마킹) 기능 — 현장에서 메모 남기기
- [ ] PWA로 전환 (홈 화면 추가, 오프라인 지원)

## 메모
- 커밋 이력: 최초 Video 뷰어 → 핀치줌 리셋 버그 수정 → Rack Layout/Connection Panels/Control 섹션 및 줌/센터링/성능 튜닝 → 도면 간 신호 상호참조(검색+점프 + 자동완성) 기능 추가 (BB1 수작업 경로 데모는 비교 후 폐기)
