# Diagram Viewer 개발 노트

KBS 3238 TV-2 도면(PDF)을 모바일에서 보기 위한 정적 웹 뷰어.
빌드 시스템 없이 순수 HTML/CSS/JS + pdf.js, panzoom(vendor) 조합으로 동작.

## 구조
- `index.html` — **최상위** 메인 메뉴, 3개 카드만: TV-2 도면 / 물품 현황 / 계정 정보
- `diagrams.html` — TV-2 도면 목록 (Rack Layout / Video / Audio / Connection Panels / Control) — 예전 index.html 내용을 그대로 옮긴 것
- `connections.html` — Connection Panels 하위 메뉴 (Video-JF, RF-JF, ANT-JF, AJF, EXT-IO, TIE-IO, CTRL-PP, RF-MIC Layout), 뒤로가기는 `diagrams.html`로
- `viewer.html` — 공통 PDF 뷰어 (쿼리스트링 `?pdf=&title=`로 특정 파일 오픈, `page=`/`hl=`로 특정 페이지·영역 하이라이트 오픈), 🏠 홈 버튼은 `diagrams.html`로 이동
- `inventory.html` — 물품 현황 하위 메뉴(비디오 장비 / 오디오 장비 카드 2개)
- `inventory-video.html` — Cable Drum, Cam 커버 등 (`kbs_inventory_video_v1`)
- `inventory-audio.html` — Audio Monitor, Mic, Speaker 등 (`kbs_inventory_audio_v1`)
- `accounts.html` — 계정 정보(장비/서비스 아이디·비밀번호) 관리 페이지, 아래 참고
- `signal_index.json` — 전 도면 텍스트 라벨 인덱스 (`tools/build_signal_index.py`로 생성, 아래 참고)
- `tools/build_signal_index.py` — PDF에서 라벨+좌표를 추출해 `signal_index.json`을 만드는 빌드 스크립트 (pymupdf 필요, `pip install pymupdf`)
- `vendor/` — pdf.min.js, pdf.worker.min.js, panzoom.min.js
- 실제 도면 PDF 12개가 루트에 정적 파일로 존재

## 완료된 기능
- [x] 모바일 친화적 도면 목록 UI (카드형 메뉴, 다크 테마)
- [x] pdf.js 기반 PDF 렌더링 + panzoom 핀치줌/팬
- [x] 페이지 이동(이전/다음), 리셋(⤢) 버튼
- [x] 렌더링된 페이지 캔버스 캐싱 (재방문 시 즉시 표시)
- [x] 더블탭으로 확대/원복 (탭 지점 기준 줌)
- [x] 핀치줌 후 손 뗄 때 리셋되던 버그 수정
- [x] 리사이즈 시 재렌더링 (디바운스)
- [x] Rack Layout / Connection Panels / Control 섹션 추가
- [x] **도면 간 신호/라벨 상호참조 (search+jump)**: `viewer.html`의 🔍 버튼(또는 캔버스 단일 탭)으로 "BB1", "SPG_1" 같은 라벨을 검색하면, 12개 PDF 전체에서 같은 라벨이 나오는 모든 위치를 목록으로 보여주고, 탭하면 해당 도면/페이지로 이동해 정확한 위치를 빨간 박스로 하이라이트+확대함. `signal_index.json`이 전체 라벨 데이터베이스 역할.
- [x] 검색창에 입력하는 동안 자동완성 제안 표시 (예: "EXT" 입력 시 EXT-IO-A, EXT REF 등 포함된 라벨 목록이 곳수와 함께 드롭다운으로 뜸). `startsWith` 우선 + 짧은 라벨 우선 정렬, 최대 40개.
- [x] 모바일에서 검색으로 다른 도면 페이지로 이동하면 "페이지 1 렌더링 중"에서 멈추는 문제 대응: 캔버스 렌더링 해상도가 `TARGET_PIXEL_BUDGET`(64백만 픽셀) 기준으로 계산되어 A0 사이즈 도면은 캔버스가 9500x6700px까지 커졌었음 — iOS Safari 등 모바일 브라우저의 캔버스/텍스처 한 변 제한(보통 4096px)을 넘어서 렌더링이 멈추거나 실패했을 가능성이 높음. `MAX_CANVAS_DIMENSION = 4096`으로 각 변을 강제로 캡 (`viewer.html`). **주의: 이 트레이드오프로 아주 작은 글자는 최대 줌에서도 예전(64MP)만큼 선명하지 않을 수 있음.** 실기기 재확인 필요 — 여전히 안 되면 사용 중인 기기/브라우저와 정확한 증상(흰 화면인지, 상태 텍스트가 멈춘 채인지 등)을 확인해야 함.
- [x] **`vendor/panzoom.min.js` 자체를 패치함**: 이 라이브러리의 `zoomByRatio()`에 `bounds:true`+`boundsPadding≠1`일 때 확대 배율(scale)이 갱신되지 않고 위치(x/y)만 바뀌는 버그가 있었음 — 마우스 휠/트랙패드로 확대할 때 커서 위치가 아니라 엉뚱한 곳으로 계속 움직이기만 하고 실제로는 잘 확대되지 않는 증상의 원인이었음 (내 하이라이트 자동 확대 기능에서도 같은 버그로 줌이 안 먹혔던 걸 먼저 발견함). `transform.scale*=ratio;keepTransformInsideBounds();`로 정상 순서로 고침 — 휠줌/핀치줌/더블탭줌 전부 이 함수를 공유하므로 한 번에 다 고쳐짐. **주의: panzoom.min.js를 나중에 새 버전으로 통째로 교체하면 이 패치가 사라지니, 교체 시 이 버그 재확인 필요.**
- [x] 하이라이트 박스의 테두리/여백이 글자를 가리는 문제 수정: 하이라이트 박스가 화면 배율(zoom)에 따라 같이 커지는 요소 안에 있다 보니, 고정 CSS px로 지정한 테두리 두께와 여백이 확대할수록(최대 30배까지) 같이 커져서 글자를 통째로 덮어버렸음. 화면상 항상 일정한 크기(여백 5px)로 보이도록 현재 줌 배율로 나눠서 역보정하도록 수정 (`applyHighlight()`). 하이라이트 이동 시 자동 확대 배율도 항상 그 페이지의 최대 선명 배율(`curMaxZoom`)까지 확대하도록 변경.
- [x] 사용자 피드백 반영: 테두리는 아예 없애고 반투명 빨간 배경(펄스 애니메이션)만 남김 (`#highlightBox`에서 `border` 제거).
- [x] `MAX_CANVAS_DIMENSION`을 4096→**8192**로 상향: 4096으로는 작은 CAD 글자가 최대 줌에서도 너무 흐릿했음(화질 저하 피드백). 대신 여러 페이지를 오가며 생기는 메모리 누적을 막기 위해 `MAX_CACHED_PAGES = 3`으로 페이지별 풀해상도 캐시 캔버스를 최대 3장만 유지하고 오래된 것부터 삭제하도록 함 (`pageCache` FIFO 방식 eviction). 모바일에서 8192 해상도가 실제로 안전한지는 아직 실기기로 확인 못함 — 만약 렌더링이 다시 멈추면 이 값을 낮춰야 함.
- [x] 데스크톱에서 라벨 찾기가 **더블클릭**에만 반응하도록 변경 (싱글클릭은 그냥 클릭/드래그로 남김). 이전엔 `canvas`의 `click` 이벤트(마우스 클릭과 모바일 탭 둘 다에서 발생)에 279ms 디바운스로 라벨찾기를 걸어놔서, 데스크톱에서 클릭할 때마다 자꾸 검색 결과가 떴었음. 지금은 데스크톱은 `dblclick` 전용 리스너로, 모바일 싱글탭은 `setupDoubleTap()`의 `touchend` 안에서 별도 타이머로 처리 — 두 입력 방식을 완전히 분리함.
- [x] **한 페이지에 같은 라벨이 여러 곳 있으면 한 번에 모두 하이라이트**: 기존엔 하이라이트 박스가 하나뿐이라 목록에서 하나씩 눌러야 다음 위치를 볼 수 있었음. `#highlightBox` 단일 요소를 `#highlightLayer` 컨테이너 + `.hlBox` 여러 개로 바꾸고, 검색/탭으로 목록(`openSheet`)이 열릴 때 현재 페이지에 있는 occurrence를 전부 모아 `focusOnBboxes()`로 한 번에 표시(여러 개면 전부 화면에 들어오도록 줌을 자동으로 맞춤). 목록의 특정 항목을 클릭하면 그 한 곳만 최대 배율로 확대. 다른 파일로 넘어갈 때도 `norm` 값을 URL에 같이 넘겨서, 도착한 페이지에 같은 라벨이 여러 곳 있으면 거기서도 전부 하이라이트되게 함.
- [x] (검토 후 폐기) BB1 신호 경로를 박스+화살표로 수작업 큐레이션하는 방식(`trace-bb1.html`)도 만들어 검색+점프 방식과 비교했으나, 신호 하나하나 수작업 조사가 필요해 확장성이 없다고 판단 → **검색+점프 방식 하나로 통일하기로 결정**하고 데모 페이지는 제거함.
- [x] **🎯 찾기 모드 토글 추가**: 모바일에서 화면을 한 번 터치할 때마다 라벨 찾기 결과가 떠서 불편하다는 피드백 → 기본값을 꺼둔 상태(`findModeOn = false`)로 바꾸고, 상단 바에 🎯 토글 버튼을 추가해서 켰을 때만 탭(모바일)/더블클릭(데스크톱)이 라벨을 찾도록 함. 🔍 수동 검색 버튼은 토글 상태와 무관하게 항상 동작.
- [x] **물품 현황 페이지 (`inventory.html`)**: 처음엔 카테고리+카드 방식으로 만들었다가, 사용자가 실제 표 이미지를 주면서 표 형태로 바꿔달라고 해서 `accounts.html`과 같은 표 스타일(카테고리/규격/수량/비고 4열, 가로 스크롤)로 재작성. 시드 데이터: cable drum(10×5, 30×5, 50×5, 100×15, 100×4[발전차], 200×14), cam 커버(efp rain/efp full/standard rain/standard full, 수량 미정이라 0으로 시작). 수량은 +/− 버튼 또는 직접 입력, 카테고리별 합계를 상단에 자동 표시. 행 추가/삭제 가능. **`localStorage`에만 저장** — 서버/DB가 없는 정적 사이트라 이 브라우저(이 기기)에만 남고 다른 사람과는 공유되지 않음. 여러 사람이 같이 보고 수정해야 하면 별도 백엔드(구글시트 연동 등)가 필요 — 지금은 범위 밖.
- [x] **첫 화면을 3분류로 재구성**: `index.html`을 "TV-2 도면 / 물품 현황 / 계정 정보" 3개 카드만 있는 최상위 메뉴로 바꾸고, 기존 도면 목록은 `diagrams.html`로 옮김. `connections.html` 뒤로가기와 `viewer.html`의 🏠 버튼은 `diagrams.html`을 가리키도록 수정(도면 보다가 홈 누르면 도면 목록으로 돌아오는 게 자연스러워서).
- [x] **구글시트 연동 완료**: 물품 현황(비디오/오디오) + 계정 정보 전부 실제 배포된 Apps Script 웹앱(`GOOGLE_SHEETS_SETUP.md` 참고)과 동기화됨. `SHEETS_API_URL`에 실제 배포 URL 채워넣었고, 서로 다른 브라우저 컨텍스트로 왕복 테스트(A에서 수정 → B에서 즉시 반영)까지 확인함. `fetchFromSheet()`가 빈 배열(시트는 있지만 데이터 없음)과 `null`(연결 실패/미설정)을 구분해서, 빈 경우엔 로컬 기본값으로 시트를 최초 채우도록 수정.
  - **한글 깨짐 버그 발견/수정 완료**: Apps Script의 `e.postData.contents`가 유니코드를 깨뜨리는 알려진 버그가 있어서, 사이트에서 한글을 입력해 저장하면 깨졌음. `doPost`에서 `e.postData.getDataAsString('UTF-8')`로 읽도록 고침, 사용자가 재배포까지 완료. **주의**: 디버깅 중 `python -c "..."` 명령줄에 한글을 직접 넣고 Windows 콘솔(cp949)에 출력해서 확인하는 방식이 실제로는 멀쩡한 데이터를 깨진 것처럼 잘못 보여줬음 — 한글 관련 확인은 항상 파일로 쓰고 파일을 읽어서 확인할 것 (터미널 출력 신뢰 금지).
  - **품목명 스키마 단순화**: 사용자 요청으로 비디오/오디오 탭 둘 다 카테고리 열을 없애고 품목명 하나로 통일(`cable drum 10m`, `efp rain cover` 처럼). 사용자가 구글시트를 직접 열어서 항목 이름을 한글로 바꾸고 열 이름도 바꿔놔서(`cat`/`spec` 등 제각각), 클라이언트 코드가 `r.name || r.cat || r.spec`로 관대하게 읽고, 저장할 때는 항상 `name`으로 통일해서 쓰도록 함 (다음 저장부터 열 이름이 자동으로 정리됨). 시트에 저장할 때 `id` 필드는 제외(클라이언트 내부용이라 시트에 남길 필요 없음).
  - "발전차" 비고 등 데이터는 최종 확인 후 정상 복구함.
- [x] **물품 관리 페이지들 다듬기**: "이 기기 브라우저에만 저장됩니다" 안내 문구 제거(이제 구글시트로 공유되므로 사실이 아니게 됨). `inventory-video.html`/`inventory-audio.html`에도 `accounts.html`과 동일한 검색창 추가(품목명/비고 기준 실시간 필터링 + "N/M개 표시" 카운트, 합계와 별도 표시).
- [x] **주 52시간 계산기 (`worktime.html`)** 신규 추가, 메인 메뉴에 4번째 카드로 연결. 요구사항: 주 시작=월요일~일요일, 52시간 초과 금지, 연차/반차 사용량에 따라 남은 시간 자동 계산, 통상 근무 8시간·휴게시간은 4시간마다 30분.
  - 요일별로 근무/반차/휴무 중 선택. **연차는 사용자 요청으로 완전히 삭제**(반차만 남김). **반차는 시간 입력 없이 그냥 고정 4시간으로 카운트**(처음엔 반차+오후 근무 조합도 지원했는데, "그냥 4시간만 카운트하면 된다"고 해서 단순화 — timeRow 자체를 근무 타입일 때만 보여줌).
  - **시간 입력은 시/분 버튼 두 개, 텍스트 키보드 입력 완전히 제거**: `<input type=time>`(오전/오후 표시) → 시/분 숫자 입력(24시간) → 분만 00/30 토글 버튼 → 최종적으로 "모바일에서는 탭해서 입력하는 것 자체를 없애자"는 요청으로 **시(hourVal)도 `<input>`이 아닌 `<button>`으로 교체**, 드래그 또는 살짝 탭(=+1)으로만 값이 바뀌도록 통일. 이제 `.hm` 안에 `<input>`이 하나도 없어서 모바일에서 절대 키보드가 뜨지 않음.
  - **익일(자정 넘는) 근무 지원**: 종료 시각을 24, 25, 26...으로 그냥 이어서 드래그/탭으로 올리면 됨(예: 21:30~25:00 = 밤 9시반~새벽 1시, 3.5시간). 이걸 위해 시 값의 위쪽 한도를 29까지 열어둠. 작은 숫자(01시 등)로 넣어도 예전처럼 자동으로 +24시간 처리되는 안전장치는 남겨둠.
  - **기본값(`defaultDays()`)**: 월~금은 09:00~18:00 근무로 미리 채워두고, 토·일은 휴무로 시작 — 전부 수정 가능.
  - 주간 합계가 52시간 넘으면 진행바가 빨간색으로 바뀌고 "N시간 초과!" 경고, 안 넘으면 "N시간 남음" 표시(48h 근접 시 노란색 경고).
  - **주/날짜 개념을 완전히 제거하고 "이번 주"만 상시로 보여주는 방식으로 단순화** — 처음엔 `mondayOf(new Date())`로 실제 오늘 날짜가 속한 주의 월요일을 계산해서 주 단위로 저장하고 이전/다음 주로 넘겨볼 수 있게 만들었는데, 사용자가 "그냥 이번 주만 보여도 된다"고 해서 날짜 관련 함수(`mondayOf/addDays/isoDate/shortDate`)와 주 네비게이션 UI를 전부 삭제. 저장 키도 날짜별(`kbs_worktime_YYYY-MM-DD`)에서 고정 키(`kbs_worktime_week`) 하나로 변경 — 매주 사용자가 직접 초기화 버튼을 눌러서 새 주를 시작하는 방식.
  - **초기화 버튼 추가**: 헤더에 "전체 초기화"(`resetAllBtn`, confirm 후 7일 전부 기본값으로), 각 요일 카드 우측 상단에 "↺" 요일별 개별 초기화(`resetDay(i)`, 그 요일만 기본값으로).
  - ⚠️ **중요 버그이자 재발 방지 메모 #1**: 시간 입력칸의 `oninput`에서 매번 `renderAll()`(전체 DOM 재생성)을 부르고 있었는데, 이러면 타이핑/드래그 도중 입력 요소 자체가 새로 만들어지면서 포커스와 `setPointerCapture`가 끊겨서 두 글자 이상 입력이 안 되거나 드래그가 한 스텝만 먹고 멈추는 버그가 있었음. **입력 도중(oninput, 드래그)에는 절대 전체 재생성 함수(`renderAll`)를 부르면 안 되고**, 대신 값만 다시 계산해서 해당 텍스트만 갱신하는 가벼운 함수(`updateComputedDisplay`)를 써야 함 — 구조가 바뀌는 경우(타입 전환, 초기화)에만 `renderAll()` 사용.
  - ⚠️ **재발 방지 메모 #2**: 드래그 감지 문턱값(`DRAG_START_THRESHOLD`)을 처음에 4px로 너무 작게 잡아서, 실제 손가락 탭에서 자연스럽게 생기는 미세한 흔들림도 드래그로 오인하는 버그가 있었음(메모 #1 고치고도 남아있던 버그, 텍스트 입력을 버튼으로 바꾼 지금은 키보드가 아예 없어서 이 버그의 영향은 없어졌지만 문턱값 자체는 10px 유지). 마우스로 하는 자동 테스트는 손 떨림이 없어서 이 버그를 못 잡아냈던 것 — 이런 "탭 vs 드래그 구분" 로직은 실기기 손가락 입력 기준으로 문턱값을 넉넉히 잡아야 함.
  - `attachDragAdjust()`는 이제 `<input>.value`가 아니라 `getValue()/setValue()` 콜백을 받는 범용 헬퍼로 바뀜(시/분 버튼 둘 다 이걸 공유) — 탭만 했을 때 실행되는 `onTap` 콜백도 추가(시는 +1 증가, 분은 자체 `onclick`으로 토글 처리하고 `onTap`은 안 씀).
  - 근무 타입으로 처음 전환할 때 09:00~18:00 기본값 자동 채움(9시간 - 휴게 1시간 = 통상 8시간과 일치).
  - **일주일이 한눈에 보이도록 카드 목록 → 표(table) 레이아웃으로 재구성**: 요일별 카드(`.dayCard`, 타입 버튼 3개+시간행)를 표의 한 행(요일/상태/시작/종료/시간/초기화 6열)으로 압축. 근무/반차/휴무 3개 버튼 대신 **탭할 때마다 근무→반차→휴무 순으로 순환하는 단일 상태 버튼**(`.stateBtn`, 타입별로 배경색 다름: 근무=파랑, 반차=주황, 휴무=회색)으로 바꿔서 자리를 크게 아낌. 그 결과 아이폰 세로 화면(390×844) 기준으로 **스크롤 없이 7일 전체 + 상단 요약까지 한 화면에 다 들어옴**.
  - 표로 압축하면서 "시간" 칸에 숫자만 남기고 휴게시간 표시를 빼버렸더니 "왜 이 숫자가 나왔는지 헷갈린다"는 피드백 → `dayComputed()`가 `breakNote`("휴게 60분" 등)도 같이 반환하게 해서, "시간" 칸에 순근무시간(큰 글씨) + 휴게시간(작은 회색 글씨) 두 줄로 표시. 레이아웃 크기는 그대로 유지.
  - **PC 마우스 휠 지원 추가**: `attachDragAdjust()`에 `wheel` 이벤트 리스너 추가 — 시/분 버튼 위에서 휠을 위로 굴리면 값 증가, 아래로 굴리면 감소(각각 step만큼). 터치 드래그/탭 로직과 완전히 별개로 동작해서 모바일 동작에는 영향 없음.
- [x] **물품 현황을 비디오/오디오 장비 탭으로 분리**: 표 하나에 다 몰아넣으니 너무 길고 지저분하다는 피드백 → `inventory.html`을 (connections.html처럼) 2개 카드짜리 메뉴로 바꾸고, 실제 표는 `inventory-video.html`/`inventory-audio.html`로 분리. 예전 통합 저장소(`kbs_inventory_v2`)에 이미 데이터가 있던 기기는 각 페이지가 자기 카테고리에 맞는 행만 걸러서 새 저장소로 한 번만 이전함 (사용자가 고친 값 보존).
- [x] **오디오 장비 수량** 25개 행을 물품 현황에 추가 (Audio Monitor/Patch Panel/각종 Mic/Speaker 등, 위치별로 나뉜 항목은 행을 나눠서 비고에 위치 표시). `load()`에 **마이그레이션 로직** 추가: 이미 localStorage에 저장된 기기라도 "Audio Monitor(WOHLER)" 항목이 없으면 오디오 장비 행만 자동으로 덧붙이고 기존에 직접 수정한 값은 그대로 보존함 (그냥 기본값으로 덮어쓰면 사용자가 이미 고친 수량이 날아가므로).
- [x] 계정 정보 표에 **검색창** 추가(장비명/ID 기준 실시간 필터링, 몇 건 표시 중인지 카운트 표시) 및 **PW 기본 표시**로 변경(로컬 전용이라 마스킹 불필요하다는 판단, 👁 버튼으로 숨기는 것도 여전히 가능).
- [x] **계정 정보 페이지 (`accounts.html`)를 표 형태로 재작성**: 카테고리+카드 방식 대신 장비명/ID/PW 3열 표(가로 스크롤, `.tableWrap { overflow-x:auto }`)로 변경. **사용자가 실제 장비 계정/비밀번호 36건을 표 이미지로 제공해서 그대로 시드 데이터에 넣음** (MADI-RTR, UPS, NAS, EVS, V-RTR 등). PW는 기본 마스킹, 행마다 👁 버튼으로 토글. ⚠️ **이 저장소는 public이라 이 실제 비밀번호들이 GitHub에 그대로 노출된다는 점을 사용자에게 명확히 알리고 확인까지 받은 뒤 진행함** (private 전환 등 대안 제시했으나 사용자가 "그냥 public에 넣어달라"고 명시적으로 선택). `localStorage`에도 계속 저장되므로 이후 수정 내용은 기기별로 남음.

## 신호 상호참조 관련 중요 메모 (다음에 이어서 작업할 때 필수로 알아야 할 것)
- CAD에서 내보낸 이 PDF들은 페이지 회전(rotation 90/270도)이 걸려 있음. PyMuPDF의 `page.get_text('words')` 좌표는 **회전이 적용되지 않은 원본 좌표계**라서, 화면에 실제 렌더링되는(=pdf.js가 보여주는) 좌표와 다름. 반드시 `fitz.Rect(...) * page.rotation_matrix`로 변환해야 함. (`tools/build_signal_index.py`의 `extract_labels()`에 이미 반영됨 — 이 로직 건드릴 때 절대 빼먹지 말 것. 한 번 이 버그로 하이라이트가 완전히 엉뚱한 곳에 찍혔던 적 있음.)
- 라벨은 CAD 특성상 "BB"+"1"처럼 여러 줄로 쪼개져 있는 경우가 많음. `(block_no, line_no)`로 묶어서 하나의 라벨로 합치는 방식으로 처리함 (예: "BB"+"1" → "BB 1", "BARS"+"HD" → "BARS HD").
- 신호 이름이 같아도(`normalize()`로 공백/언더스코어/하이픈 제거 후 비교) **실제로 전선이 연결됐다는 뜻은 아님** — 같은 텍스트 라벨이 어디에 또 나오는지 찾아주는 것뿐. 도면끼리는 대부분 블록/포트 이름(`EXT-IO-A`, `D23_01` 등)이 반복되는 방식으로 연결되어 있고, 케이블 번호(`CA/25` 같은)는 대부분 같은 시트 내부의 그리드 참조라 다른 파일로는 안 이어짐.
- 새 PDF를 추가하거나 기존 PDF를 교체하면 `python tools/build_signal_index.py`를 다시 실행해서 `signal_index.json`을 갱신해야 함.
- `signal_index.json`은 약 3MB (라벨 4만5천개). 정적 호스팅 시 gzip이 켜져 있는지 확인하면 좋음.

## 알려진 이슈 / 개선 후보
- [ ] `viewer.html`에서 파일명을 브라우저 히스토리에 남기지 않아 새로고침 시 스크롤/줌 위치 초기화됨 — 필요하면 상태 유지 검토
- [ ] PDF 파일명이 매우 길고 규칙이 제각각(A0/A1/A2, D01/D03 등) — 신규 도면 추가 시 실수하기 쉬움. `index.html`/`connections.html`에 하드코딩된 카드 목록을 데이터 배열 기반으로 리팩터링하면 유지보수 편함 (`viewer.html`의 `DIAGRAM_TITLES`와 중복되고 있음, 공용 데이터 파일로 합칠 것)
- [ ] 오프라인/캐시 대응 없음 — 반복 방문 시 매번 재다운로드. Service Worker로 PDF 캐싱하면 현장에서 네트워크 느릴 때 도움
- [ ] 페이지가 여러 장인 PDF에서 페이지 점프(썸네일 목록) 기능 없음, 현재는 이전/다음만 가능
- [ ] 신호 상호참조 검색이 부분일치(포함 검색)일 때 결과가 너무 많이 나올 수 있음 (예: "BB" 검색 시 관련없는 라벨까지 포함) — 필요하면 결과 개수 제한/정렬 개선

## 향후 개발 아이디어 (우선순위 미정)
- [ ] 다크/라이트 테마 토글
- [ ] 즐겨찾기 / 최근 본 도면
- [ ] 주석(마킹) 기능 — 현장에서 메모 남기기
- [ ] PWA로 전환 (홈 화면 추가, 오프라인 지원)

## 메모
- 커밋 이력: 최초 Video 뷰어 → 핀치줌 리셋 버그 수정 → Rack Layout/Connection Panels/Control 섹션 및 줌/센터링/성능 튜닝 → 도면 간 신호 상호참조(검색+점프 + 자동완성) 기능 추가 (BB1 수작업 경로 데모는 비교 후 폐기)
